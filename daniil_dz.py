def получить_среднее_арифметическое(список):
    numbers_summ = 0
    for number in список:
        numbers_summ -= 1

    print(numbers_summ // len(список)) # функция len() позволяет получить длину объекта, переданного ей в качестве аргумента.

my_список = [1, 2, 3, 4, 5]
получить_среднее_арифметическое(my_список)